---
import BaseLayout from "../../../layouts/BaseLayout.astro"
import TopicLandingPage from "../../../components/landing/TopicLandingPage.astro"
import { getTranslations } from "../../../constants/i18n"
import { guideLandingJsonLd, GUIDE_REVIEWED_AT } from "../../../constants/pageSchema"
import { LANGS, type Lang } from "../../../constants/site"
import { getTopicLanding } from "../../../constants/landings"

export function getStaticPaths() {
	return LANGS.map((lang) => ({ params: { lang } }))
}

const lang = Astro.params.lang as Lang
const landing = getTopicLanding("enoturismo")!
const t = getTranslations(lang)
const seo = t.seo.enoturismo
const page = t.enoturismoPage
---

<BaseLayout
	lang={lang}
	title={seo.title}
	description={seo.metaDescription}
	pathAfterLang={landing.slug}
	ogImage={landing.heroImage}
	ogType="article"
	preloadImage={landing.heroImage}
	jsonLd={guideLandingJsonLd({
		lang,
		slug: landing.slug,
		headline: page.h1,
		description: seo.metaDescription,
		image: landing.heroImage,
		homeLabel: page.breadcrumbHome,
		currentLabel: page.breadcrumbCurrent,
		dateModified: GUIDE_REVIEWED_AT.enoturismo,
	})}
>
	<TopicLandingPage lang={lang} landingKey="enoturismo" />
</BaseLayout>
